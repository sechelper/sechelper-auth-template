# API

Go 1.26.8 backend for the Confidential Client authentication shell. The composition root is `cmd/server`; platform infrastructure is under `internal/platform`; authentication and Manifest capabilities are module-owned under `internal/modules`.

Canonical setup and deployment instructions live under the repository `docs/` directory.
