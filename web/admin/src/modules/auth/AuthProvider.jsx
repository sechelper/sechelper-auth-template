import { createContext, useContext, useEffect, useMemo, useState } from "react";
import { authApi } from "./api.js";
import { oidcAccountURL } from "../../platform/config/runtime.js";
const AuthContext = createContext(null);
export function AuthProvider({ children }) { const [state, setState] = useState({ status: "loading" }); const refresh = async () => { try { const session = await authApi.session(); if (!session.authenticated) { setState({ status: "unauthenticated" }); return; } const [account, authorization] = await Promise.all([authApi.account(), authApi.authorization()]); setState({ status: "authenticated", ...session, user: account.data, ...authorization.data, permissions: authorization.data?.permissions || [] }); } catch { setState({ status: "error" }); } }; useEffect(() => { refresh(); }, []); const value = useMemo(() => ({ state, user: state.user || null, userCenterURL: () => oidcAccountURL(), hasPermission: (permission) => state.permissions?.includes(permission) || false, login: authApi.login, refresh, refreshSession: async () => { await authApi.refresh(); await refresh(); }, logout: async () => { await authApi.logout(); setState({ status: "unauthenticated" }); window.location.assign("/"); } }), [state]); return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>; }
export function useAuth() { return useContext(AuthContext); }
