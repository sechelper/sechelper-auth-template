# First-run application configuration.
# Sensitive runtime variables are supplied through the deployment environment;
# keep only non-secret startup defaults in this file.
bootstrap:
  installLockFile: install.lock
app:
  environment: development
  listenAddr: 127.0.0.1:8080
