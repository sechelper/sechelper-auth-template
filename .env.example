# Compose infrastructure interpolation only. Application runtime values live
# in config.yaml and are entered through /install.
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_DB=auth_template
